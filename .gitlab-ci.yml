image: "node:10.15.0-alpine"

before_script:
  - pwd
  - ls -al
  - apk update
  - node -v
  - npm -v
  - cd client/www && yarn install

build:
  stage: build
  script:
    - yarn run build

tslint:
  stage: test
  script:
    - yarn run lint

test:
  stage: test
  script:
    - yarn run test:ci
